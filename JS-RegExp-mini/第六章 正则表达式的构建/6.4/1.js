const regex = /\d+/g;

const str = "123abc34def";

console.log(regex.lastIndex, regex.exec(str));
console.log(regex.lastIndex, regex.exec(str));
console.log(regex.lastIndex, regex.exec(str));
console.log(regex.lastIndex, regex.exec(str));